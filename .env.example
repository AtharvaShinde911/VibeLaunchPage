MONGODB_HOST=your-mongodb-host:27017
MONGODB_USER=your_username
MONGODB_PASSWORD=your_password
MONGODB_DB_BACKEND=proddb

# Or use a full connection string instead:
# MONGODB_URI=mongodb://user:password@host:27017/proddb

# Server port (optional — use 8080 if you prefer)
PORT=8080
